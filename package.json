{
  "name": "cliowrapper",
  "displayName": "Clio Wrapper",
  "description": "Wrapper for CI/CD tools.",
  "icon": "images/icon.png",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Clio Wrapper",
      "properties": {
        "cw.outputPath": {
          "type": "string",
          "default": "C:\\",
          "scope": "resource",
          "description": "Specify output full path for .gz file."
        },
        "cw.remoteTestServerLogin": {
          "type": "string",
          "default": "",
          "scope": "resource",
          "description": "Bpmsoft account login for test server."
        },
        "cw.remoteTestServerPassword": {
          "type": "string",
          "default": "",
          "scope": "resource",
          "description": "Bpmsoft account password for test server."
        }
      }
    },
    "commands": [
      {
        "command": "cliowrapper.create",
        "title": "Create Package"
      },
      {
        "command": "cliowrapper.test.createandsend",
        "title": "Create Package And Send To Test Server"
      }
    ],
    "menus": {
      "explorer/context": [
          {
              "when": "explorerResourceIsFolder",
              "command": "cliowrapper.create"
          },
          {
            "when": "explorerResourceIsFolder",
            "command": "cliowrapper.test.createandsend"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "@typescript-eslint/eslint-plugin": "^6.15.0",
    "@typescript-eslint/parser": "^6.15.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3",
    "@vscode/test-cli": "^0.0.4",
    "@vscode/test-electron": "^2.3.8"
  },
  "extensionDependencies": [
    "vscode.git"
  ]
}
